---
type Props = PropsWithChildren<BaseProps<HTMLDivElement>>

const {class: className, ...rest} = Astro.props    
---

<div class:list={[styles['Hero-eyebrow'], className]} {...rest}>
  <slot />
</div>
      
    